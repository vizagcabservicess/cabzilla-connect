import { createNavigationContainerRef } from '@react-navigation/native';

/** Used for navigating from push handlers / overlays outside screen components. */
export const navigationRef = createNavigationContainerRef();
