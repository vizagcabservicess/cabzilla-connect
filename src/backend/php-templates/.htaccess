
# Main .htaccess - Primary configuration for frontend and API routing
<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Enable error logging
    RewriteLog "/var/log/apache2/rewrite.log"
    RewriteLogLevel 3
    
    # Handle CORS preflight requests first
    RewriteCond %{REQUEST_METHOD} OPTIONS
    RewriteRule ^(.*)$ $1 [R=200,L]
    
    # Add CORS headers 
    Header always set Access-Control-Allow-Origin "*"
    Header always set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
    Header always set Access-Control-Allow-Headers "*"
    
    # Handle assets directory directly - bypassing PHP
    RewriteRule ^assets/(.*)$ assets/$1 [L]
    
    # Step 1: API requests route to api directory first (highest priority)
    # This ensures API calls work correctly
    RewriteRule ^api(/.*)?$ api$1 [L]
    
    # Step 2: For real files/directories EXCEPT /admin path, serve directly
    RewriteCond %{REQUEST_URI} !^/admin(/.*)?$
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]
    
    # Step 3: Critical - Special handling for /admin routes (must come before general handling)
    # This ensures admin routes are handled by the SPA router
    RewriteCond %{REQUEST_URI} ^/admin(/.*)?$
    RewriteRule ^ index.php [L]
    
    # Step 4: All other requests go to index.php (SPA router)
    # This ensures React Router handles all frontend routes
    RewriteRule ^ index.php [L]
</IfModule>

# General headers
<IfModule mod_headers.c>
    # Cache control for HTML and PHP
    <FilesMatch "\.(html|php)$">
        Header set Cache-Control "no-store, no-cache, must-revalidate, max-age=0"
        Header set Pragma "no-cache"
        Header set Expires "0"
    </FilesMatch>
    
    # Allow caching for assets
    <FilesMatch "\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$">
        Header set Cache-Control "public, max-age=86400"
    </FilesMatch>
</IfModule>

# PHP settings
<IfModule mod_php7.c>
    php_flag display_errors On
    php_value error_reporting E_ALL
    php_flag log_errors On
    php_value error_log /tmp/php_errors.log
</IfModule>
