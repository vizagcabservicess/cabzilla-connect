
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function PayrollEntryForm() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Payroll Entry Form</CardTitle>
      </CardHeader>
      <CardContent>
        <div>Payroll entry form will be implemented here.</div>
      </CardContent>
    </Card>
  );
}
