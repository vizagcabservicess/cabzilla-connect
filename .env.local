
VITE_GOOGLE_MAPS_API_KEY=AIzaSyDqhYmgEp_DafM1jKJ8XHTgEdLXCg-fGy4
VITE_API_BASE_URL=https://vizagup.com

# Note: The API path is appended in the API service
# Updated timestamp for cache busting during API requests
VITE_API_VERSION=1.0.60

# Direct API access - set to true to bypass CORS proxy
VITE_USE_DIRECT_API=true

# Force cache refresh on load
VITE_FORCE_CACHE_REFRESH=true

# Force use of latest schema columns
VITE_USE_UPDATED_SCHEMA=true

# Enable airport transfer tier pricing
VITE_USE_AIRPORT_TIERS=true

# Disable CORS proxy for API calls
VITE_USE_CORS_PROXY=false

# CORS debug settings
VITE_CORS_DEBUG=true
VITE_CORS_DEBUG_LOG=true

# Use fix-cors.php endpoint before other API calls
VITE_USE_CORS_FIX=true

# Increase max retry attempts for vehicle operations
VITE_MAX_VEHICLE_RETRIES=5

# Enable offline fallback for vehicle operations
VITE_ENABLE_OFFLINE_FALLBACK=true

# Add auto sync settings for vehicle tables
VITE_AUTO_SYNC_VEHICLE_TABLES=true
VITE_USE_DIRECT_VEHICLE_API=true
